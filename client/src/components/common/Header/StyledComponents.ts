import { styled } from '@mui/material';

const Container = styled('div', { name: 'header__container' })`
  display: flex;
  padding: 10px 20px;
  background: #b7b7ff;
  align-items: center;
  width: 100%;
  justify-content: space-between;
`;

export default Container;
